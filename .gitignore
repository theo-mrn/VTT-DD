# ── Dépendances ────────────────────────────────────────────────────────────────
node_modules
.pnpm-store/

# ── Builds et caches (régénérables) ────────────────────────────────────────────
dist/
.next/
out/
build/
.turbo/
*.tsbuildinfo
next-env.d.ts
coverage/

# ── Tests ──────────────────────────────────────────────────────────────────────
test-results/
playwright-report/
blob-report/
playwright/.cache/
# Session Playwright enregistrée (jeton de connexion)
legacy/src/__tests__/playwright/helpers/.session.json

# ── Secrets : jamais dans git ──────────────────────────────────────────────────
.env*
!.env.example
*.pem
certificates/

# ── Système et éditeurs ────────────────────────────────────────────────────────
.DS_Store
*.log
.vercel

# ── Ancienne app (legacy) : médias lourds, servis depuis R2 ───────────────
legacy/public/Map/
legacy/public/Cartes/
legacy/public/Photos/
legacy/public/Token/
legacy/public/items/
legacy/public/Musics/
# Modèle 3D de 65 Mo : trop lourd pour git sans LFS (hébergé sur R2)
legacy/starwars-bundle/assets/3d/
# Bundles générés (pnpm bundle:zip)
*-bundle.zip
